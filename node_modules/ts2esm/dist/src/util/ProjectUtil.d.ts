import { Project, SourceFile } from 'ts-morph';
export declare const ProjectUtil: {
    getPaths: (project: Project) => import("ts-morph").ts.MapLike<string[]> | undefined;
    getProject: (tsConfigFilePath: string) => Project;
    getRootDirectory: (tsConfigFilePath: string) => string;
    getTsConfigFilePath: (sourceFile: SourceFile) => string;
};
