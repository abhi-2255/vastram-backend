import { Expression, Statement } from 'ts-morph';
export declare class NodeUtil {
    static hasOnlyLineBreaks(input: string): boolean;
    static extractComment(input: Statement | Expression): {
        comment: string;
        hasNoText: boolean;
        statement: string;
    };
}
