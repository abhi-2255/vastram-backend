import path from 'node:path';
import { Project } from 'ts-morph';
export const ProjectUtil = {
    getPaths: (project) => {
        return project.getCompilerOptions().paths;
    },
    getProject: (tsConfigFilePath) => {
        return new Project({
            skipFileDependencyResolution: true,
            tsConfigFilePath,
        });
    },
    getRootDirectory: (tsConfigFilePath) => {
        return path.dirname(tsConfigFilePath);
    },
    getTsConfigFilePath: (sourceFile) => {
        return sourceFile.getProject().compilerOptions['_settings'].configFilePath + '';
    },
};
//# sourceMappingURL=ProjectUtil.js.map