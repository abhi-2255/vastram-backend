import type { StringLiteral } from 'ts-morph';
export type ModuleInfo = {
    declaration: string;
    directory: string;
    extension: string;
    quoteSymbol: string;
    normalized: string;
    pathAlias: string;
    isRelative: boolean;
    sourceFilePath: string;
};
export declare function getNormalizedDeclaration(stringLiteral: StringLiteral): string;
export declare function parseInfo(sourceFilePath: string, stringLiteral: StringLiteral, paths: Record<string, string[]> | undefined): ModuleInfo;
