import { diffString } from 'json-diff';
export function getJsonDiff(a, b) {
    const diff = diffString(a, b);
    if (diff.length === 0) {
        return null;
    }
    return diff;
}
//# sourceMappingURL=getJsonDiff.js.map